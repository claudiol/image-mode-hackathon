---
- name: Deploy Image Mode image to development
  hosts: all
  gather_facts: true
  become: true

  tasks:
    - name: Placeholder - deploy development
      ansible.builtin.debug:
        msg:
          - "Deploy Image Mode image to development placeholder."
